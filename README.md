# Tradersecho

The current working preview is in [twitter-sentiment-app-v1.5.3](twitter-sentiment-app-v1.5.3/README.md), using the new `backend.service:app` entrypoint.

See that README for the dashboard, account tiers, live X collector, local preview, deployment and outstanding launch configuration. Earlier folders are retained as history.

The old root environment and login document are removed in this branch. Rotate any real credentials they contained; deletion does not remove Git history.
